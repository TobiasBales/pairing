<div class="card">
  <div class="card-body">
    <h5 class="card-title">
      <%= team.name %>
    </h5>
    <div class="card-body">
      <table class="table">
        <thead>
          <th></th>
          <% team.members.reverse.each do |tm| %>
            <th scope="col"><%= tm.name %></th>
         <% end %>
        </thead>
        <tbody>
          <% team.members.each do |tm| %>
            <tr>
              <th scope="row"><%= tm.name %></th>
              <% team.members.reverse.each do |partner| %>
                <% if tm != partner %>
                  <td>x</td>
                <% else %>
                  <td></td>
                <% end %>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
